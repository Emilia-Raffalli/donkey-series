{% extends 'base.html.twig' %}

{% block title %} Index {% endblock %}

{% block body %}


<div>
    <img class='head mb-3' src="{{ latestProgram.poster }}" alt="{{ latestProgram.title }}" style="width: 100%; height: 500px; object-fit: cover;">
    <div class="overlay-text latestProgram">
        <h1>{{ latestProgram.title }}</h1>
        <p>{{ latestProgram.synopsis }}</p>
        <div>
            <a href="#" class="btn btn-primary">Lire</a>
            <a href="{{ path('app_program_show', {'id': latestProgram.id}) }}" class="btn btn-primary">En savoir plus</a>
        </div>

        
    </div>

</div>

<div class='container flex flex-wrap p-3'>
    {% for program in programs %}
        <div class="card m-3" style="width: 18rem;">
            <img class="card-img-top" src="{{ program.poster }}" alt="{{ program.title }} poster" style="object-fit: cover; width: 100%; height: 200px;">
            <div class="card-body">
                <h2 class="card-title">{{ program.title }}</h2>
                <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
                <a href="{{ path('app_program_show', {'id': program.id}) }}" class="btn btn-primary">Découvrir</a>
            </div>
        </div>
    {% endfor %}
</div>




{# <table>
    <tr>
        <th>{{ knp_pagination_sortable(programs, 'Id', 'p.id') }}</th>
        <th{% if programs.isSorted('p.title') %} class="sorted"{% endif %}>
            {{ knp_pagination_sortable(programs, 'Title', 'p.title') }}
        </th>
    </tr> #}

    {# {% for program in programs %} #}
        {# <tr {% if loop.index is odd %}class="color"{% endif %}>
            <td>{{ program.id }}</td>
            <td>{{ program.title }}</td>
            <td>{{ program.synopsis }}</td>
        </tr> #}
    {# {% endfor %} #}
{# </table> #}





{# display pagination navigation #}
 <div class="navigation">
        {{ knp_pagination_render(programs) }}
</div>





{% endblock %}

{# {{ loop.index }}  #}